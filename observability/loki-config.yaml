# Loki minimal single-binary konfiguratsiyasi (compose ichida).
# Kichik loyihalar uchun mo'ljallangan — hech qanday tashqi bog'liqlik yo'q,
# barcha ma'lumot lokal disk'da.
auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9096
  log_level: warn

common:
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

limits_config:
  # Katta so'rovlarga xavfsizlik chegarasi (event vaqtida foydali).
  retention_period: 720h            # 30 kun log tarixi
  ingestion_rate_mb: 8              # 8 MB/sek yozish (kichik loyiha uchun yetarli)
  ingestion_burst_size_mb: 16
  reject_old_samples: true
  reject_old_samples_max_age: 168h  # 7 kundan eski log'ni qabul qilmaydi
  max_query_series: 10000

compactor:
  working_directory: /loki/compactor
  retention_enabled: true
  delete_request_store: filesystem
  retention_delete_delay: 2h
  retention_delete_worker_count: 50

query_range:
  results_cache:
    cache:
      embedded_cache:
        enabled: true
        max_size_mb: 100

analytics:
  reporting_enabled: false
