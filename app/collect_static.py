"""
Placeholder for collecting custom static files.
starlette-admin serves its own built-in statics automatically — no manual
collection is needed for the admin panel.

Add entries here if you later introduce project-level static assets
(custom CSS, fonts, images, etc.).
"""

if __name__ == "__main__":
    print("No custom statics to collect.")
